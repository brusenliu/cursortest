# Low-memory tuning for 1 core / 2 GB VPS (Netdata v2.x)
# Installed to /etc/netdata/netdata.conf.d/lowmem.conf

[db]
    update every = 2s
    db = dbengine
    dbengine page cache size = 32MiB
    dbengine multihost disk space = 256MiB

[web]
    bind to = localhost
